dbt-project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres         # atau IP host database Anda
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow
      schema: public
      threads: 1
